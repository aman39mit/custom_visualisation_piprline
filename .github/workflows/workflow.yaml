name: Pull Request

on:
  workflow_dispatch:

jobs:
  checkout-and-build-pr:
    name: checkout-and-build-pr
    runs-on: ubuntu-latest
    steps:
      # - name: Checkout repo
      #   uses: actions/checkout@v2

      # - name: Setup node
      #   uses: actions/setup-node@v1
      #   with:
      #     node-version: 14

      # - name: Cache node_modules
      #   id: cache-node-modules
      #   uses: actions/cache@v1
      #   env:
      #     cache-name: node-modules
      #   with:
      #     path: ~/.npm
      #     key: ${{ runner.os }}-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
      #     restore-keys: |
      #       ${{ runner.os }}-${{ env.cache-name }}-

      - name: Install Dependencies
        run: npm ci

      - name: Install NR1 CLI
        run: curl -s https://cli.nr-ext.net/installer.sh | sudo bash

      - name: Save credentials 
        run: nr1 profiles:add --name {3933528} --api-key {NRAK-G9JMX1EYJEC7JLFP77V0XFJJ1RA} --region "us"

      # - name: Create your package
      #   run: nr1 create --type nerdpack --name my-awesome-nerdpack

      - name: Start developing
        run: cd my-awesome-nerdpack && nr1 nerdpack:serve

      - name: NR1 Nerdpack Build
        run: |
          nr1 nerdpack:build

  # test:
  #   name: test
  #   needs: checkout-and-build-pr
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repo
  #       uses: actions/checkout@v2

  #     - name: Setup node
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: 14

  #     - name: Cache node_modules
  #       id: cache-node-modules
  #       uses: actions/cache@v1
  #       env:
  #         cache-name: node-modules
  #       with:
  #         path: ~/.npm
  #         key: ${{ runner.os }}-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
  #         restore-keys: |
  #           ${{ runner.os }}-${{ env.cache-name }}-

  #     - name: Install Dependencies
  #       run: npm ci

  #     - name: Run npm test
  #       run: npm test

  # validate-nerdpack:
  #   name: validate nerdpack
  #   needs: checkout-and-build-pr
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repo
  #       uses: actions/checkout@v2

  #     - name: Validate Open Source Files
  #       uses: newrelic/validate-nerdpack-action@v1

  #     - name: Install NR1 CLI
  #       run: |
  #         curl -s https://cli.nr-ext.net/installer.sh | sudo bash

  #     - name: Validate Nerdpack Schema
  #       run: |
  #         nr1 nerdpack:validate